# ── Schwab API (existing from old app) ──
SCHWAB_APP_KEY=
SCHWAB_APP_SECRET=
SCHWAB_REFRESH_TOKEN=

# ── Finnhub (existing from old app) ──
FINNHUB_API_KEY=

# ── Polygon.io (existing from old app) ──
POLYGON_API_KEY=

# ── Anthropic Claude API (NEW — needed for /analyze deep dive) ──
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ── SEC EDGAR (FREE — just needs a user-agent identity string) ──
EDGAR_IDENTITY=your.email@example.com
